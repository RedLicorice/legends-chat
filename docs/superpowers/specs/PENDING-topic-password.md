# Pending: Topic Password Protection

Feature requested during hashtag brainstorm session (2026-05-09).

## Requirements
- Admin can set a password on a topic
- User must input password once to join/access the topic
- Configurable time between required password re-entries (e.g., re-enter every 7 days)
- Configurable behavior on password change: require re-entry immediately vs. on next session

## To be designed
Full brainstorm session needed before implementation.

---

# Pending: Admin User Activity Log

Feature requested 2026-05-09.

## Requirements
- In admin user details view, show activity log for that user
- Uses available data (existing events/logs in DB or derivable from messages, bans, etc.)
- Default: last 30 events
- Configurable limit

## To be designed
Full brainstorm session needed before implementation.
